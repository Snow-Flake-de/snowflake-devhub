# API Module

Route handlers in `server/src/routes` form the current API layer.

This folder exists for gradual extraction into dedicated API controllers
and versioned contracts (`v1`, `v2`) as the platform evolves.
